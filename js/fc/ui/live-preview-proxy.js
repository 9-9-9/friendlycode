"use strict";

define(function() {
  return function() {
    throw new Error("editor-side cross-origin live preview proxy is " +
                    "unimplemented; see examples/sandboxed-preview for " +
                    "a proof-of-concept implementation.");
  };
});
